/**
 * Created by orel- on 08/Dec/15.
 */
import alt from '../alt';

class HomeActions {
    constructor() {
    	/*
    	Here you can generate 'actions' which will just pass down values to listeners in store
    	You just need to specify the names, and then attach the listener with 'onNameOfYourGeneratedAction' to store
    	For example:
        this.generateActions(
        	'changeWhatever'
        );

        When you have that, you will be able to attach 'onChangeWhatever' to your store and pass down a value to it;
        You can consider it a setter
        */
    }

    test() {

    }
}

export default alt.createActions(HomeActions);
